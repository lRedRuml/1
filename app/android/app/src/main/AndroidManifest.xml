<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- [ИСПРАВЛЕНО] Release-манифест (в отличие от debug/profile —
         см. src/debug и src/profile) НЕ получает INTERNET автоматически.
         Без этого разрешения релизная сборка (та, что реально ставят на
         телефон и публикуют) не может выполнить НИ ОДИН сетевой запрос —
         ни к /api/v1 (логин, ключи, серверы, тарифы), ни сам VPN-туннель:
         кнопка "Подключить" технически не может ничего сделать, а
         "серверы не пингуются" — ровно то поведение, которое даёт
         SecurityException при попытке открыть сокет без этого разрешения.
    -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <!-- [НОВОЕ] Требуется для реального списка приложений на экране
         split-tunnel (installed_apps, см. NATIVE_SETUP.md и
         screens/split_tunnel_screen.dart) — без него
         InstalledApps.getInstalledApps() возвращает пустой список/кидает
         исключение. tools:ignore — чувствительное разрешение, Google Play
         спросит обоснование при публикации (см. NATIVE_SETUP.md). -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission"/>
    <application
        android:label="vpnonline_app"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>

