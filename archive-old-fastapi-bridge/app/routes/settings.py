"""
Публичные настройки бота/сайта, безопасные для показа в приложении — v4
[НОВОЕ]. Раньше приложение либо хардкодило эти значения (текст "триал не
настроен" в menu_screen.dart), либо не показывало их вовсе (проценты
реферальной программы). Теперь единая точка правды — то же самое
bot_settings, что видит бот.

Эндпоинт БЕЗ авторизации намеренно: эти значения нужны ещё ДО входа в
аккаунт (например текст на экране регистрации "минимум 6 символов" не
отсюда, а вот "пробный период 2 дня" на онбординге — да), и в них нет
ничего чувствительного (это не токены/пароли, см. shop_db.get_public_settings
— явный whitelist ключей, секреты туда физически не могут попасть).
"""
from fastapi import APIRouter
from app.services import shop_db

router = APIRouter()


@router.get("")
async def get_public_settings():
    return await shop_db.get_public_settings()
