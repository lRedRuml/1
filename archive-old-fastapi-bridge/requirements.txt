fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
email-validator==2.2.0
python-dotenv==1.0.1
httpx==0.27.2
pyjwt==2.9.0
sqlalchemy[asyncio]==2.0.35
asyncpg==0.29.0
aiosqlite==0.20.0
# v4: email+пароль как на реальном сайте (см. routes/auth.py) — формат
# хэшей в реальном backup БД (users.password_hash, "$2b$10$...") — это
# bcrypt, поэтому используется официальный пакет bcrypt, а не что-то
# самописное поверх hashlib.
bcrypt==4.2.0
